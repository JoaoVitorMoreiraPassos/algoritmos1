#include <stdio.h>
 
int main() {
 
    float n[100];
    int c;
    double
    
    for(c = 0; c < 100; c++){
        if(c == 0){
            scanf("%lf",&x);
        }
        n[c] = x;
        printf("N[%d] = %.4lf\n",c,n[c]);
        x = x / 2;
    }
 
    return 0;
}
